apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "dummy2.fullname" . }}
  labels:
    app: {{ include "dummy2.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "dummy2.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "dummy2.name" . }}
    spec:
      containers:
      - name: {{ include "dummy2.name" . }}-container
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        ports:
        - containerPort: {{ .Values.containerPort }}
